import {ProtectedRoute} from "~/routes/ProtectedRoute";


export default function JobDetails({ job } : {job: any} ){

    return (
        <ProtectedRoute>
            <div>

            </div>
        </ProtectedRoute>
    )
}